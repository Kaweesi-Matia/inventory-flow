# Empty = same-origin /api, proxied by Vite to the backend (local dev).
VITE_API_BASE_URL=

# Production (Vercel): full backend origin, no trailing slash, e.g.
# VITE_API_BASE_URL=https://supplychainx-api.onrender.com
